/**
 * rootColors.ts — корневые справочные цвета, определяющие визуальный характер бренда
 *
 * Не применяются напрямую. Используются только как источник для генерации растяжек (Palette).
 *
 * Набор содержит минимальный спектр и необходимые цвета для всего UI:
 * - фонов, текстов, ошибок и других элементов интерфейса
 * - проходит сквозняком по всем брендам
 *
 * Система построена на трёх логических уровнях:
 * ↴ root — базовые цвета (этот файл)
 * ↴ palette — формирует тональные диапазоны
 * ↴ colors — семантический слой, является поставщиком цветовых схем
 */

export const rootColors = {
  purple: '#652dd2', // Основной акцентный цвет (education)
  indigo: '#5355a8', // Основной акцентный цвет (FTData)
  blue: '#3d52b6', // Информационные сообщения
  green: '#236e44', // Успех и подтверждения
  red: '#b00823', // Ошибки и предупреждения
  orange: '#a66517', // Предупреждения
  yellow: '#b38800', // Дополнительные предупреждения
  neutral: '#575c75', // Нейтральная палитра
} as const;
